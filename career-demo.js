(()=>{const root=document.querySelector('[data-career-demo]');if(!root)return;const tabs=[...root.querySelectorAll('.demo-tab')],stages=[...root.querySelectorAll('.demo-stage')],prev=root.querySelector('[data-prev]'),next=root.querySelector('[data-next]'),progress=root.querySelector('[data-progress]');let index=0;function show(i){index=Math.max(0,Math.min(stages.length-1,i));tabs.forEach((t,n)=>{t.classList.toggle('active',n===index);t.setAttribute('aria-selected',n===index?'true':'false')});stages.forEach((s,n)=>s.classList.toggle('active',n===index));prev.disabled=index===0;next.disabled=index===stages.length-1;progress.textContent=`Step ${index+1} of ${stages.length}`;}tabs.forEach((t,n)=>t.addEventListener('click',()=>show(n)));prev.addEventListener('click',()=>show(index-1));next.addEventListener('click',()=>show(index+1));show(0);})();