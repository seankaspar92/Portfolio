(()=>{document.querySelectorAll('[data-career-demo]').forEach(root=>{const tabs=[...root.querySelectorAll('.demo-tab')],stages=[...root.querySelectorAll('.demo-stage')],prev=root.querySelector('[data-prev]'),next=root.querySelector('[data-next]'),progress=root.querySelector('[data-progress]');let index=0;function show(i){index=Math.max(0,Math.min(stages.length-1,i));tabs.forEach((t,n)=>{t.classList.toggle('active',n===index);t.setAttribute('aria-selected',n===index?'true':'false')});stages.forEach((s,n)=>s.classList.toggle('active',n===index));if(prev)prev.disabled=index===0;if(next)next.disabled=index===stages.length-1;if(progress)progress.textContent=`Step ${index+1} of ${stages.length}`;}tabs.forEach((t,n)=>t.addEventListener('click',()=>show(n)));if(prev)prev.addEventListener('click',()=>show(index-1));if(next)next.addEventListener('click',()=>show(index+1));show(0);});const hero=document.querySelector('.case-hero');if(hero&&!document.querySelector('[data-live-scan-link]')){const row=document.createElement('div');row.className='button-row';row.setAttribute('data-live-scan-link','');row.style.marginTop='18px';row.innerHTML='<a class="button" href="career-scan.html">Run your own resume + job scan →</a>';hero.appendChild(row);}})();